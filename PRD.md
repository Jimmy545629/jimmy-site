# PRD.md — 「来源看板」产品需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名 | **来源看板** |
| 所属项目 | `jimmy-site`（https://github.com/Jimmy545629/jimmy-site） |
| 文档版本 | v1.2（含 AI 自检后修订） |
| 日期 | 2026-09-19 |
| 作者 | Jimmy |
| 状态 | 待 Day 5 技术选型后进入开发 |
| 上游文档 | `research.md`（Day 3 需求研究，本文档的所有取舍都以它为依据） |
| 下游文档 | Day 5 技术选型；Day 7 上线 MVP |

> **这份文档要解决什么**：`research.md` 回答的是「做什么、不做什么」，本文档回答的是
> 「**具体做成什么样**」——有哪几个页面、每个功能怎么操作、数据存什么、**怎么算做完了**。
>
> 读本文档的人不需要读过 `research.md`；凡是需要依据的地方，本文档会直接标出 `research.md` 的对应小节。

---

## 1. 背景与问题

### 1.1 我遇到的问题

我有几个固定会去看的地方（技术站点、资讯站点）。问题是：

- **记不住上次看到哪**。同一个站点，我不确定上周那篇是不是已经看过了。
- **忘了去看**。没有任何机制提醒我「这个来源你两周没打开了」。
- **现成工具不对味**。要么替我自动抓（抓什么、抓多少、要不要收费，全在别人手里），要么是个越攒越乱的收藏夹。

### 1.2 为什么不用现成的（`research.md` 第 2 节摘要）

| 路线 | 代表 | 为什么不适合我 |
| --- | --- | --- |
| RSS 阅读器 | Feedly、Inoreader | 免费档刻意做窄（Feedly 100 源 / 3 文件夹 / **无搜索**）；消费版正被企业版挤走（官网主推 $1,600/月起） |
| 收藏 / 稍后读 | Pocket、Raindrop.io | **Pocket 已于 2025-07-08 关停，导出窗口 2025-10-08 过后数据永久删除**；Raindrop 组织得漂亮，但回答不了「这个来源我多久没看了」 |
| 自建 / 开源 | FreshRSS、Miniflux、NetNewsWire | 数据确实在自己手里，但要一台一直开着的机器 + 会部署 + 自己管备份，门槛太高 |

**更关键的一条**：这个品类里死掉的产品不止一个（Google Reader 2013、Omnivore 2024-11、Pocket 2025-07、Tiny Tiny RSS 2025-11）。
**头号死因不是功能不够，是「服务先死了」，而用户存的东西可能连搬家的机会都没有。**

### 1.3 所以我要做的

> 一个只给自己用的网页：我盯 **3–5 个固定来源**，去看过之后把自己发现的东西贴进来；
> 网页按来源归好、排成时间线，并且告诉我「**哪个来源我已经好久没去看了**」。
> 数据存在**我自己的项目里**。

---

## 2. 目标与非目标

### 2.1 这一版（Day 7 MVP）要达成的目标

| 编号 | 目标 | 怎么算达成 |
| --- | --- | --- |
| G1 | 打开网页，一眼看清每个来源「最后记录是几天前」 | 见 AC-01、AC-02、AC-03 |
| G2 | 能在一分钟内把「刚看到的一条东西」贴进去 | 见 AC-05、AC-06 |
| G3 | 数据完全在我自己手里，不依赖任何服务方的存活 | 见 AC-11、AC-12 |
| G4 | 用满两周后，我确实靠它补看了几个差点漏掉的来源 | 主观判断，两周后回看（不在 Day 7 验收范围内） |

### 2.2 非目标（本版明确不做）

`research.md` 第 5 节列了 15 项「本期不做」，全部继续有效。其中对本 PRD 影响最大的 5 条，在这里再点一遍：

| 不做 | 为什么（简述，详见 `research.md` 第 5 节） |
| --- | --- |
| **自动抓取 / RSS 订阅** | 纯网页受浏览器同源策略限制，抓不了别人的网站；要抓必须有服务器，服务器要 Day 23 之后才有 |
| **超过 5 个来源** | 硬性锁死。来源一多我就不会挨个看，产品也就失去意义 |
| **标签体系 / 全文搜索 / AI 摘要** | 一共 3–5 个来源，靠「按来源看」+「按时间排」就够找；标签会变成第二套负担 |
| **账号注册 / 登录 / 多人使用** | 只给自己用 |
| **推送 / 邮件提醒 / 手机 App** | 用「来源久没记录」这件事本身来提示，够用 |

> ⚠️ **本文档第 5 节的功能清单，与上面这张表不冲突**。凡是本表里说"不做"的东西，
> 第 5 节里也没有对应条目——这是刻意的，验收时（AC-14）会逐条核对。

### 2.3 三条硬约束（不可让步）

这三条是设计的前提，Day 5 技术选型和 Day 7 开发都必须满足：

| 编号 | 约束 | 为什么不可让步 |
| --- | --- | --- |
| C1 | **不依赖任何服务器**。整个网站不需要后端、不需要数据库、不需要联网即可使用 | Pocket 的教训：服务方一旦死掉，数据可能连导出都来不及（`research.md` 3.0 节第 1 行） |
| C2 | **数据必须能变成一个我能直接打开、能直接拷走的文件** | 同上。这是「数据在我手里」这句话唯一可验证的形式 |
| C3 | **来源数量硬性上限 5 个** | 来源一多就不会挨个看（`research.md` 改变 4） |

---

## 3. 用户与使用场景

### 3.1 用户

**唯一用户：我自己（Jimmy）。** 没有第二个角色，没有管理员，没有访客。
因此本 PRD 里不出现「权限」「角色」「多人协作」这类内容。

### 3.2 使用的设备与环境

- 主要：**Windows 电脑上的 Chrome / Edge**，双击打开网页。
- 次要：手机上用浏览器打开同一个文件（能用即可，不专门做手机界面）。
- 网络：**有没有网都要能用**（约束 C1）。

### 3.3 三个核心场景

| 场景 | 什么时候发生 | 我用它做什么 | 对应功能 |
| --- | --- | --- | --- |
| **S1　贴一条**（最高频） | 我刚在某个来源看到一条有用的东西 | 打开网页 → 点那个来源的「记录一条」→ 填标题和链接 → 保存 | F3 |
| **S2　补看**（本产品的价值所在） | 我打开网页，看到某个来源标红了 | 点来源卡片上的地址，去那个来源看看 → 回来贴 1–3 条 | F2、F1 |
| **S3　回看** | 我想找前阵子记的一条 | 在时间线里往下翻，或筛选到某个来源，或点进来源详情页 | F4、F5 |

### 3.4 一次典型的完整使用（走一遍 S2）

1. 打开网页 → 首页顶部写着「**你现在最该看的是：掘金（11 天没记录了）**」
2. 来源卡片里「掘金」是标红的，旁边写着「该看了 · 最后记录：11 天前」
3. 点卡片上的地址 → 新标签页打开掘金 → 我翻一会儿，挑出 2 条
4. 回到网页，点「记录一条」→ 选掘金 → 填两条 → 保存
5. 「掘金」变成「最后记录：今天」，红色标记消失；顶部那句话换成下一个该看的来源

> 这个流程一共 5 步，其中只有第 3、4 步需要我动脑。**产品要做的就是把第 1、2、5 步自动化。**

---

## 4. 数据模型

只存两样东西：**来源**（Source）和**条目**（Item）。来源是主，条目挂在来源下面。

### 4.1 来源（Source）

| 字段 | 必填 | 类型 / 限制 | 说明 |
| --- | --- | --- | --- |
| `id` | 是 | 系统生成 | 内部标识，我不需要看见 |
| `name` | 是 | 文字，最多 20 字 | 比如「GitHub Trending」「36氪」 |
| `url` | 否 | 网址 | 用来一键点过去看。留空也能建，但会少一个便利 |
| `createdAt` | 是 | 日期时间 | 系统自动填，用于跟「加入顺序」排序 |

**派生值（不单独存，每次打开时算出来）**：

| 派生值 | 怎么算 | 用在哪 |
| --- | --- | --- |
| `lastRecordedAt` | 该来源下**最新的一条条目的记录时间**；一条都没有则为空 | 首页「最后记录」 |
| `daysSinceLastRecord` | 今天减去 `lastRecordedAt` 的**自然日**天数；取整 | 「X 天前」、是否标红 |
| `itemCount` | 该来源下的条目数 | 来源卡片上的「N 条」 |

### 4.2 条目（Item）

| 字段 | 必填 | 类型 / 限制 | 说明 |
| --- | --- | --- | --- |
| `id` | 是 | 系统生成 | 内部标识 |
| `sourceId` | 是 | 指向某个来源 | 必须选一个来源 |
| `title` | 是 | 文字，最多 100 字 | 我看到的那个东西叫什么 |
| `url` | 否 | 网址 | 原文地址，可留空 |
| `note` | 否 | 文字，最多 200 字 | **我的一句话**（为什么值得记） |
| `recordedAt` | 是 | 日期时间 | 默认"现在"，**允许改成别的日期**（理由见 4.4） |

> **字段数量是刻意的**：就这三个内容字段（来源 / 标题 / 链接 + 一句话），不再多。
> 这正是 2026-09-19 确认过的决定。每多一个输入框，我贴东西的意愿就低一分。

### 4.3 两种数据的关系

```
来源 Source（最多 5 个）
   │
   ├── 条目 Item
   ├── 条目 Item          ← 每个条目必须属于某一个来源
   └── 条目 Item
```

- 一个来源可以有 0 条、1 条或很多条条目。
- 一个条目**只能**属于一个来源（不做「一个条目挂多个来源」，那样就变成标签了）。
- 删掉一个来源时，它下面的条目会**跟着一起删**（处理规则见 F7）。

### 4.4 两个已经定下来的细节（说明理由）

**① 为什么「记录时间」允许改？**

因为产品最有用的那个数字是「**最后记录：X 天前**」。如果时间不准，这个数字就不可信，整个产品的核心价值就塌了。
典型情况：我周一在掘金看到一篇，周三才想起来贴——默认的"现在"会让它记成周三，
而「我这次到底看没看掘金」这件事只发生在这一次，改一次日期比让数据长期失真划算。

**② 为什么「我的一句话」是可以留空的？**

因为它虽然最有价值，但**强制填写会拦住我**。宁可先让我把标题贴进来，
也不要因为「懒得写感想」而整条不记。留空不报错，只是卡片上少一行字。

---

## 5. 功能需求

优先级说明：**P0 = Day 7 必须做完**；**P1 = 有时间就做，做不完不算 Day 7 没完成**。

### 5.1 功能总表

| 编号 | 功能 | 优先级 | 一句话 |
| --- | --- | --- | --- |
| F1 | 来源看板（首页来源区） | **P0** | 首页直接列出全部来源，每个都带「最后记录」 |
| F2 | 久未记录提示 | **P0** | 超过 7 天没记录（或从未记录）的来源要一眼看出来 |
| F3 | 新增一条记录 | **P0** | 选来源 + 填标题，两步完成 |
| F4 | 时间线 | **P0** | 全部条目按时间从新到旧 |
| F5 | 按来源筛选时间线 | **P0** | 只看某一个来源的条目 |
| F6 | 新增来源 + 5 个上限 | **P0** | 加到第 6 个时明确拒绝并说明原因 |
| F7 | 重命名 / 删除来源 | P1 | 删除需二次确认 |
| F8 | 编辑 / 删除条目 | P1 | 删除需二次确认 |
| F9 | 数据文件：保存 + 导出 | **P0** | 数据能变成一个我能直接打开、直接拷走的文件 |
| F10 | 数据导入（还原） | P1 | 换电脑或数据丢失时用导出的文件还原 |

---

### F1　来源看板（首页来源区）　【P0】

**做什么**：首页最上面一块，横向或纵向列出我建的全部来源（3–5 个）。

**每个来源卡片上显示**：

| 位置 | 内容 | 备注 |
| --- | --- | --- |
| 主行 | 来源名称 | |
| 主行右侧 | 「N 条」 | 该来源下的条目总数 |
| 第二行 | **「最后记录：X 天前」** | 没有条目时显示「还没有记录过」 |
| 第二行右侧 | 来源地址（可点击） | 点开＝新标签页去看了。地址为空就不显示 |
| 操作 | 「记录一条」按钮 | 直接带着这个来源打开新增表单 |

**排序**：按**加入顺序**（`createdAt`）固定排列。

> **为什么不用「最久没记录排最前」**：卡片位置每天跳来跳去，反而会让我找不到某个来源。
> 「哪个最该看」这个信息，用顶部的提示语 + 标红来表达（见 F2），不靠排序。

**边界情况**：

- 一个来源都还没建 → 首页显示引导语「先添加你想盯的来源（最多 5 个）」+「添加来源」按钮。
- 来源没有地址 → 卡片第二行右侧不显示，其余照常。
- 来源名称过长 → 卡片内截断显示（鼠标悬停可看全称）。

---

### F2　久未记录提示　【P0】

**做什么**：把「哪个来源我好久没看了」这件事做成一眼可见。

**两条输出**：

1. **来源卡片标红**：当 `daysSinceLastRecord >= 7`，或该来源**从未记录过**时，
   卡片出现明显的视觉标记，并且**同时带文字**「**该看了**」。
   > 只靠颜色是不够的——颜色要配文字，否则色觉差异或打印时会丢失信息。
2. **首页顶部一句话**：指出**当前最该看的那个来源**，格式：
   「你现在最该看的是：**掘金**（11 天没记录了）」——点击可以直接打开该来源的地址。

**7 天这个数字**：写在代码里的一处常量，不做设置页。**理由**：整个网站只给我自己用，
多一个设置项就多一份维护成本；这个数字以后想改，改一行代码就行。
（`research.md` 里「12 天没记录」只是一个举例，不是规定。）

**边界情况**：

- **所有来源都在 7 天内记录过** → 顶部那句话改为「所有来源都在 7 天内记录过，暂时不用补看」，
  并且**不**指定某个来源。**不能出现「最该看的是 XXX（0 天没记录）」这种傻话。**
- **多个来源同时超标** → 顶部只指出**天数最多**的那一个；如果天数相同，取加入顺序更早的。
- **所有来源都从未记录过** → 顶部指出加入顺序最早的那个。
- 一个来源都没有 → 顶部提示语不显示。

---

### F3　新增一条记录　【P0】

**做什么**：把我刚看到的东西贴进来。这是使用频率最高的操作。

**表单字段**：

| 字段 | 必填 | 默认值 | 输入方式 |
| --- | --- | --- | --- |
| 来源 | 是 | 如果是从某个来源卡片点进来的，默认就是它 | 下拉，列出全部来源 |
| 标题 | 是 | 空 | 单行文字 |
| 链接 | 否 | 空 | 单行文字（网址） |
| 我的一句话 | 否 | 空 | 多行文字 |
| 记录时间 | 否 | **现在** | 日期；想补记时才改（见 4.4） |

**保存后的效果（这一条很重要，是"做对了"的判据）**：

1. 该条**立刻出现在时间线最顶部**；
2. 对应来源的「最后记录」**立刻变成「今天」**；
3. 如果这个来源原来是标红的，**红色标记立刻消失**；
4. 首页顶部那句话**重新计算**，可能换成另一个来源。

**边界情况**：

- 标题为空 → 不允许保存，提示「标题不能空着」。
- 一个来源都没有 → 「记录一条」不可点，提示先添加来源。
- 链接填了但不是合法网址 → 仍然允许保存（我可能贴的是自己记的半截地址），只是在页面上不做成可点击链接。

---

### F4　时间线　【P0】

**做什么**：首页下半部分，把全部来源的条目混在一起，**按记录时间从新到旧**排。

**每条显示**：来源名（带颜色区分）· 标题 · 我的一句话 · 记录时间 · 链接（可点击）。

**边界情况**：

- 一条条目都没有 → 显示「还没有记录。去看一个来源，把看到的东西贴进来」。
- 同一天的多条 → 按保存的先后顺序排列，不能乱。

---

### F5　按来源筛选时间线　【P0】

**做什么**：时间线上方一排筛选按钮「全部 / GitHub Trending / 掘金 / …」，点一下只看那一个来源。

> **这一条不是「搜索」，也不是「标签」**，请注意区分：
> `research.md` 第 5 节砍掉的是**全文搜索**（按关键词找正文）和**标签体系**（第二套分类法）。
> 「按来源看」恰恰是 `research.md` 第 4 节写明的导向——**来源本来就是这个产品唯一的组织单位**。

**边界情况**：筛选结果为空 → 显示「这个来源还没有记录」。

---

### F6　新增来源 + 5 个上限　【P0】

**做什么**：添加一个我要盯的来源。

**表单字段**：名称（必填，最多 20 字）、地址（可选）。

**5 个上限的行为**（硬约束 C3）：

- 已有 5 个来源时，「添加来源」按钮**变成不可点**，旁边显示一行说明：
  「**已经 5 个了。来源一多就不会挨个看——想加新的，先去掉一个。**」
- 说明文字里要**同时给出出路**（想加就得先删），否则我会以为产品坏了。

**边界情况**：

- 名称重复 → 允许（可能就是想分两个，比如「掘金-前端」「掘金-AI」）。
- 名称空白或超过 20 字 → 不允许保存，并说明原因。

---

### F7　重命名 / 删除来源　【P1】

**做什么**：修改来源名称；删掉不再盯的来源。

**删除规则**（涉及数据安全，写得细一点）：

1. 删除前弹出二次确认；
2. 确认框里**必须写明会连带删除多少条记录**，例如：
   「删除「掘金」会**同时删除它下面的 23 条记录**，删掉后找不回来。确定吗？」
3. 来源下**没有**条目 → 确认框文案简化为「删除「XXX」？」，风险提示不需要那么重。
4. 删除后，首页顶部提示语和时间线都要立刻重算。

> 约束 C2（数据要能拷走）是这条规则的前提：**删掉真的找不回来**，
> 所以确认框必须把后果说清楚，而不是弹一个「确定吗？」让人闭眼点。

**边界情况**：删掉最后一个来源 → 回到「一个来源都没有」的引导状态（见 F1）。

---

### F8　编辑 / 删除条目　【P1】

**做什么**：改一条已经记下的内容（比如发现标题写错了、想补一句感想）；或删掉记错的。

- 编辑：复用 F3 的表单，字段和校验规则完全一致。
- 删除：二次确认，文案里带上被删条目的标题，避免点错行。
- 编辑或删除后，「最后记录」和标红状态都要**重新计算**——
  > 注意：如果删掉的正好是某个来源最新的一条，那个来源的「最后记录」会**往回退**，
  > 甚至可能重新变成标红状态。这是正确行为，不是 bug。

---

### F9　数据文件：保存 + 导出　【P0】

**做什么**：让「数据在我手里」这句话变成看得见、摸得着的东西。

**要求**（写要求，不写技术做法——技术做法是 Day 5 的事）：

| 编号 | 要求 |
| --- | --- |
| F9-1 | 我关掉浏览器、第二天再打开，**之前记的内容一条不少** |
| F9-2 | 我能随时**导出一个文件**，双击就能打开看，内容是能读懂的（不是乱码） |
| F9-3 | 那个文件**能直接拷到 U 盘 / 别的文件夹**，拷走就是完整的备份 |
| F9-4 | 全程**不联网**也能完成以上三件事 |
| F9-5 | 不出现任何第三方账号、登录、云同步的入口 |

**边界情况**：数据为空时导出 → 也允许，导出一个空结构的文件（这样格式是确定的）。

---

### F10　数据导入（还原）　【P1】

**做什么**：把 F9 导出的文件再读回来，用于换电脑、或者浏览器数据被清掉之后恢复。

**要求**：导入前**明确提示会覆盖当前数据**，需要二次确认。

---

## 6. 页面结构

一共 **3 个页面**（含 1 个弹窗式表单）。刻意少——少一个页面就少一堆要维护的东西。

### 6.1 P1　首页（看板）

从上到下三块：

| 区块 | 放什么 | 对应功能 |
| --- | --- | --- |
| 顶部提示条 | 「你现在最该看的是：XXX（N 天没记录了）」 | F2 |
| 来源区 | 全部来源的卡片 + 「添加来源」按钮 | F1、F2、F6 |
| 时间线区 | 筛选按钮一排 + 全部条目（从新到旧） | F4、F5 |

**跳转**：

- 点来源卡片的**名称或「N 条」** → 进**来源详情页**（P2）
- 点来源卡片的**「记录一条」** → 打开新增表单（P3），来源已预选
- 点来源卡片的**地址** → 新标签页打开该来源（这是"去看"的动作）
- 点条目的**链接** → 新标签页打开原文

### 6.2 P2　来源详情页

| 区块 | 放什么 |
| --- | --- |
| 顶部 | 来源名称、地址（可点）、「最后记录：X 天前」、「N 条」 |
| 操作 | 「记录一条」「重命名」「删除来源」 |
| 主体 | 该来源下的全部条目，按时间从新到旧 |
| 返回 | 回到首页 |

### 6.3 P3　新增 / 编辑条目（弹窗）

字段见 F3。底部两个按钮：**保存** / **取消**。
从来源详情页打开时，来源预选为该来源；从首页打开时，来源默认为空，需要我选。

### 6.4 页面关系

```
        ┌──────────────────────────────────────┐
        │  P1 首页                              │
        │  顶部提示条 → 来源区 → 时间线区        │
        └───────┬───────────────────┬──────────┘
                │ 点来源名            │ 点「记录一条」
                ▼                    │
        ┌───────────────────┐        │
        │  P2 来源详情页     │        │
        └───────┬───────────┘        │
                │ 点「记录一条」      │
                └────────┬───────────┘
                         ▼
              ┌─────────────────────┐
              │  P3 新增 / 编辑条目  │  ← 弹窗，保存后回到来的那个页面
              └─────────────────────┘
```

---

## 7. 验收标准

> **这一节是 Day 7 / Day 23 判断「做完了没有」的唯一依据。**
> 每一条都必须是**能亲眼看到、能判断真假**的，不允许出现「体验流畅」这种没法验证的话。
>
> **优先级标注**：AC-01 到 AC-11 是 P0，Day 7 必须全部通过；
> AC-12 到 AC-14 中标注 P1 的项，做不完不算 Day 7 未完成。

### 7.1 首页与「久未记录」提示

| 编号 | 验收标准 | 级别 |
| --- | --- | --- |
| **AC-01** | 打开首页，**不需要任何点击**，就能看到我建的全部来源（3–5 个），每个来源的名称、条目数、「最后记录：X 天前」都在卡片上 | P0 |
| **AC-02** | 某个来源一条记录都没有时，它的卡片上显示的是「**还没有记录过**」，而不是「0 天前」或空白 | P0 |
| **AC-03** | 「最后记录」达到 7 天（含）以上、或从未记录过的来源，卡片上同时出现**颜色标记**和「**该看了**」**文字**——只有颜色没有文字，不算通过 | P0 |
| **AC-04** | 首页顶部显示一句话指出最该看的来源，格式为「你现在最该看的是：XXX（N 天没记录了）」；**当所有来源都在 7 天内记录过时，这句话变成「所有来源都在 7 天内记录过，暂时不用补看」，并且不指出任何具体来源** | P0 |

### 7.2 记录与时间线

| 编号 | 验收标准 | 级别 |
| --- | --- | --- |
| **AC-05** | 能新增一条记录：来源必选、标题必填、链接和「我的一句话」留空也能保存成功 | P0 |
| **AC-06** | 保存一条记录后，**立刻**发生三件事：① 它出现在时间线最顶部；② 对应来源的「最后记录」变成「今天」；③ 该来源原来的红色标记消失 | P0 |
| **AC-07** | 时间线按记录时间**从新到旧**排列，不需要刷新页面 | P0 |
| **AC-08** | 时间线上方有筛选按钮，点某一个来源后，时间线只剩那个来源的条目；点「全部」恢复到全部条目 | P0 |
| **AC-09** | 标题留空时无法保存，并且页面上给出「标题不能空着」这类的提示文字 | P0 |

### 7.3 来源管理

| 编号 | 验收标准 | 级别 |
| --- | --- | --- |
| **AC-10** | 已建满 5 个来源时，**不能**再添加第 6 个；并且页面上有一句文字说明为什么不能加、以及想加该怎么办 | P0 |
| **AC-11** | 删除一个还有条目的来源时，必须先出现二次确认，且确认文字里**写明会连带删除多少条记录** | P0 |
| **AC-12** | 能重命名来源、能编辑和删除条目；删除条目也有二次确认 | P1 |

### 7.4 数据在不在我手里

| 编号 | 验收标准 | 级别 |
| --- | --- | --- |
| **AC-13** | 记几条内容后关掉浏览器，重新打开网页，**之前记的内容一条不少** | P0 |
| **AC-14** | 能导出一个文件，**双击就能打开**、内容能读懂，并且这个文件**拷到别的位置仍然是完整的一份数据** | P0 |
| **AC-15** | **断网状态下**（拔掉网线 / 关掉 Wi-Fi）打开网页，AC-01 到 AC-14 涉及的功能全部照常可用 | P0 |

### 7.5 不许出现的东西

| 编号 | 验收标准 | 级别 |
| --- | --- | --- |
| **AC-16** | 页面上**不出现**以下任何功能或入口：注册 / 登录 / 账号、自动抓取 / RSS 订阅、标签、全文搜索框、AI 摘要或问答、网页永久快照、邮件或推送提醒、手机 App 或小程序入口 | P0 |
| **AC-17** | 页面上**不出现**任何第三方服务的登录按钮、云同步开关或付费入口 | P0 |

> **AC-16 为什么也算「验收标准」**：这个产品最大的风险不是做不出来，而是**顺手做多了**。
> `research.md` 第 5 节那 15 条，每一条都有它被砍掉的理由；
> 开发时"顺手加上去"一个搜索框，就是在毁掉前面所有的研究。所以最后的验收必须逐条对着看。

---

## 8. 风险与技术待定项

### 8.1 必须由 Day 5 决定的（本文档故意不写）

| 编号 | 待定项 | 为什么留给 Day 5 | 选型时必须满足 |
| --- | --- | --- | --- |
| T1 | 数据具体怎么存（浏览器本地存储？直接读写的文件？） | 属于技术选型，不属于产品设计 | **C1、C2 两条硬约束**，以及 AC-13、AC-14、AC-15 |
| T2 | 3 个页面是做成 1 个 HTML 文件还是 3 个 | 同上 | 双击一个文件就能用，不需要装任何东西 |
| T3 | 数据文件的格式和字段名 | 同上 | 双击能打开、人能读懂（F9-2） |
| T4 | 7 天这个阈值写在哪 | 同上 | 只有一处，好改 |

> ⚠️ **给 Day 5 的提醒**：约束 C1 说的是「不需要服务器」。
> 如果某个方案需要起一个本地服务才能用，那它**不满足 C1**，因为那就变成「要会部署」了——
> 正是 `research.md` 里否定掉路线三的那个理由。

### 8.2 已知风险

| 编号 | 风险 | 影响 | 应对 |
| --- | --- | --- | --- |
| R1 | 「数据能直接拷走」在纯网页里可能有限制 | 关系到 C2 能不能成立 | Day 5 必须先把这一条验证一遍（哪怕只做一个小样）；如果做不成，退回到「**一键导出 + 我一个星期手动存一次**」，并在 PRD 里更新 F9 |
| R2 | 国内网络访问 GitHub 不稳定（Day 3 已实际踩到 `ERR_CONNECTION_TIMED_OUT`） | 打卡截图可能看不到线上文件 | 继续用本地渲染版网页截图（Day 3 已验证有效） |
| R3 | Day 7 时间紧，P1 项可能做不完 | AC-12 过不了 | 可接受——P1 本来就不算 Day 7 的完成标准 |
| R4 | 开发时「顺手加功能」 | 破坏 AC-16 | 每加一个功能，先回 `research.md` 第 5 节对一遍 |
| R5 | 我自己用两周后觉得没用 | 白做 | 这正是 G4 要验证的。**如果两周后完全没打开过，那就承认判断错了，而不是继续加功能** |

### 8.3 后续版本（明确不在本期）

| 版本 | 可能做的事 | 前提 |
| --- | --- | --- |
| Day 23 之后 | 接数据库（`research.md` 与 `README.md` 里都提到这个节点） | 先有服务器 |
| 第二版之后 | 自动抓取某个来源（这时才有服务器能做） | 得先证明第一版我确实在用 |
| 未定 | 「记录时间」自动取当天？阈值可调？ | 用满两周后按实际体验再定 |

---

## 附录 A　术语表

| 词 | 大白话解释 |
| --- | --- |
| 来源 | 我固定会去看的那个地方，比如「掘金」「36氪」 |
| 条目 | 我在某个来源看到、并贴进来的一条东西 |
| 时间线 | 全部条目混在一起、按时间从新到旧排成的列表 |
| 二次确认 | 弹一个框问「真的要删吗」，防止手滑 |
| 同源策略（CORS） | 浏览器的一条安全规矩：网页只能读取自己这边的东西，不能去别的网站抓数据。这是「第一版不能自动抓取」的技术原因 |
| 硬约束 | 不管怎么实现都必须满足的条件，做不到就得改方案，而不是改约束 |
| 自然日天数 | 「今天」减「最后记录那天」的天数，比如今天 19 号、最后记录 12 号，就是 7 天 |

---

## 附录 B　用户故事（Day 4 余力加练）

### B.1 「久未记录提示」的完整用户故事

> **作为**　`来源看板` 的唯一使用者（我自己），
> **我想要**　在打开网页的第一眼，就看出「这几个来源里，哪个是我最久没去过的」，
> **以便于**　直接奔着那个来源去补看，而不是把 3–5 个来源挨个翻一遍、或者干脆忘了这件事。

**它解决的痛点是**：`research.md` 里已经把「自动抓取」砍掉了，
也就是说「**不会漏**」这件事，只能靠「**知道该去看哪个了**」来补。
所以这个功能不是锦上添花，**它是替代掉「自动抓取」的那根柱子**。

**这个故事的验收标准（Given / When / Then）**：

| 编号 | Given（前提） | When（操作） | Then（应该看到） |
| --- | --- | --- | --- |
| B1-1 | 「掘金」最后记录在 11 天前 | 打开首页 | 掘金卡片上有颜色标记 + 「该看了」文字，并显示「最后记录：11 天前」 |
| B1-2 | 有「掘金 11 天」、「36氪 8 天」、「GitHub Trending 1 天」三个来源 | 打开首页 | 顶部写「你现在最该看的是：**掘金**（11 天没记录了）」——**只指出天数最多的那一个** |
| B1-3 | 三个来源的最后记录分别是 1 天前、3 天前、5 天前 | 打开首页 | 顶部写「所有来源都在 7 天内记录过，暂时不用补看」，**不点名任何来源** |
| B1-4 | 「掘金」标红，我刚才去看了并贴了 1 条 | 保存这条记录 | 掘金变回正常颜色，顶部提示语换成下一个最该看的来源 |
| B1-5 | 刚建了一个新来源，还没贴过任何东西 | 打开首页 | 该来源显示「还没有记录过」并标红，**不显示「0 天前」** |

> **B1-1 到 B1-5 对应第 7 节的 AC-02、AC-03、AC-04、AC-06。**
> 写用户故事的价值就在这里：它逼我把「一遍用起来顺不顺」拆成一条一条能验证的动作。

### B.2 其余功能的一行式故事

| 功能 | 作为我自己，我想要…… | 以便于…… |
| --- | --- | --- |
| F3 新增记录 | 贴一条东西时只需要选来源 + 写标题 | 我在地铁上想起来也能 10 秒记完 |
| F4 时间线 | 所有来源的东西按时间排在一起 | 我能按「最近看到什么」的顺序回看 |
| F5 按来源筛选 | 只看某一个来源的全部条目 | 我能判断这个来源这段时间到底给了我什么 |
| F6 5 个上限 | 系统替我拦住第 6 个来源 | 我不用靠自制力，产品结构本身就挡住了「贪多」 |
| F9 数据导出 | 一键把数据变成一个能打开的普通文件 | 我不需要相信任何人的服务器能活多久 |

> **用户故事的格式**：`作为〔谁〕，我想要〔做什么〕，以便于〔为什么〕`。
> 加上 Given/When/Then 就变成可验证的验收标准——这是它比功能清单更好用的地方。

---

## 附录 C　AI 自检记录（Day 4 板块③）

**自检方式**：拿这份 PRD 对着 Day 4 的完成标准逐条核，再回答三个问题——
① 有没有写进 `research.md` 明确砍掉的功能？② 有没有写着需要服务器才能做到的功能？
③ 每条验收标准是不是「能亲眼看到、能判断真假」？

### C.1 第一轮自检：发现的问题

| 编号 | 发现的问题 | 级别 | 处理 |
| --- | --- | --- | --- |
| P1 | F5「按来源筛选」很容易被误读成「加了搜索 / 标签」——而这两样正是 `research.md` 明确砍掉的。原文没有解释这个区别 | **必须修改** | 已在 F5 里加说明框，明确区分「按来源看」与「全文搜索 / 标签体系」，并指向 `research.md` |
| P2 | 「记录时间」原本写死为保存时间。但产品最能用的数字就是「最后记录：X 天前」，时间不准这个数字就不可信；而且我常常是隔一两天才补记 | **必须修改** | 改为「默认现在，**允许改成别的日期**」，并在 4.4 节写明理由 |
| P3 | 首页顶部提示语没有覆盖「所有来源都很新」的情况，按原逻辑会显示「最该看的是 XXX（0 天没记录）」这种傻话 | **必须修改** | 已在 F2 边界情况与 AC-04 里补上规则 |
| P4 | 5 个来源上限只写了「不能加」，没写「想加怎么办」。用户会以为产品坏了 | **必须修改** | 已在 F6 里要求：拒绝的同时必须给出路（先去掉一个），AC-10 同步补上 |
| P5 | 缺少「来源没有地址」「一个来源都没有」这些初始与空状态 | 建议修改 | 已补进 F1、F3、F4、F5 的边界情况 |
| P6 | 第 5 节功能清单没有优先级，Day 7 时间不够时不知道砍哪个 | 建议修改 | 已给每个功能标 P0 / P1，AC 也标了级别 |
| P7 | 「删掉来源后，条目怎么办」没有交代 | 建议修改 | 已在 F7 写明连带删除 + 二次确认文案要求 |
| P8 | 数据存放这一块原本混在功能里，容易被当成"技术细节"忽略掉，可它其实是约束 C1/C2 的落点 | 建议修改 | 提为独立功能 F9，并把 C1/C2/C3 前置到第 2.3 节 |
| P9 | 附录 A 术语表可以直接复用 | 可以忽略 | — |

### C.2 第二轮自检：结果

针对第一轮 4 个「必须修改」逐条复核后的结论：

| 检查项 | 结论 |
| --- | --- |
| 有没有写进 `research.md` 砍掉的功能？ | **没有。** 全文只有 F5 涉及"筛选"，已明确说明它属于「以来源为单位」而不是搜索/标签。AC-16 把 15 项「不做」里出现频率最高的 8 项直接写成了验收条目 |
| 有没有需要服务器才能做到的功能？ | **没有。** F1–F10 全部是本地数据的增删改查 + 导出导入；唯一有技术不确定性的是 T1（数据怎么存），已作为待定项交给 Day 5，并写了 R1 作为兜底 |
| 每条验收标准能不能判断真假？ | **能。** AC-01 到 AC-17 每一条都有可观察的对象（页面上的哪块、显示什么文字、能不能点）；已删除"体验流畅"这类无法验证的表述 |
| 每条功能有没有边界情况？ | **有。** F1–F10 每一条都写了边界情况；空状态（没有来源 / 没有条目 / 筛选结果为空）全部覆盖 |
| 和 `research.md` 有没有冲突？ | **没有。** 第 2.2 节 5 条、第 5 节 15 项、第 4 节的 5 条「方向」逐条比对过，无冲突；附录 D 有对应表 |
| 优先级有没有标？ | **有。** 功能 F1–F10、验收 AC-01 到 AC-17 都标了 P0 / P1 |

**第二轮自检结论：0 个「必须修改」级问题。**

### C.3 仍然存在的已知不确定性（诚实记录，不算「必须修改」）

| 编号 | 事项 | 为什么不升级为「必须修改」 |
| --- | --- | --- |
| U1 | 约束 C2（数据能变成一个能直接拷走的文件）在纯网页里能不能完全做到，要 Day 5 验证 | 这是技术待定项 T1，已写进第 8 节，并配了 R1 兜底方案。PRD 只负责把要求写清楚 |
| U2 | 手机上打开的效果只能"能用即可" | 已在 AC 层面不提高要求，避免 Day 7 被一个非核心指标拖住 |
| U3 | G4（两周后是否真的变得更有用）无法在 Day 7 验证 | 属于产品判断，写进第 8.3 作为后续复核点 |

---

## 附录 D　与 `research.md` 的对应关系

| `research.md` 的内容 | 落到本 PRD 哪里 |
| --- | --- |
| 第 0 节：一句话说清要做什么 | 第 1.3 节 |
| 第 2 节：三条路线对比 | 第 1.2 节（摘要表） |
| 第 3 节：4 条「想法改变」 | 第 2.3 节三条硬约束（改变 1 → C1/C2；改变 4 → C3） |
| 第 4 节：这一版大致长这样（5 条方向） | 逐条落到 F1、F2、F3、F4、F9 |
| 第 5 节：15 项「本期不做」 | 第 2.2 节（挑 5 条展开）+ AC-16（逐条核对） |
| 第 6.2 节：待核实项 | 第 8.2 节 R5，不当作设计依据 |

---

**文档状态**：Day 4 产出（2026-09-19）｜v1.2，已通过 AI 自检（0 个必须修改级问题）
**下一步**：Day 5 技术选型——先把待定项 T1（数据怎么存）验证清楚，因为它直接决定 C2 成不成立。
