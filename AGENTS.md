# AGENTS.md — 给 AI 的施工规则（jimmy-site）

> **这个文件是什么**：我（Jimmy）和 AI 协作时的**硬性规则**。
> 它不是说明书，也不是需求文档——它是**给 AI 看的边界线**：什么必须做、什么绝对不许做。
>
> **AI 每次开工前要先读这个文件**；如果我的要求跟这里冲突，**先指出来问我**，不要自己判断。
>
> | 项目 | 内容 |
> | --- | --- |
> | 所属项目 | `jimmy-site`（https://github.com/Jimmy545629/jimmy-site） |
> | 产品 | **来源看板** |
> | 文件版本 | v1.0（Day 6 建立） |
> | 日期 | 2026-09-21 |
> | 上游文档 | `PRD.md`（要做什么）、`TECH_DESIGN.md`（怎么做） |
>
> **建立说明**：本文件在 Day 6 建立时，项目里查不到 Day 1 写入的规则文件
> （整个仓库和整个 WorkBuddy 目录下都没有 `AGENTS.md`）。
> 按当天清单的**卡住降级方案**处理：重建规则文件，把 Day 1 该有的规则补齐，
> 再追加我自己的新规则。Day 1 写入的「当天只做当天任务」规则**原样保留**（见第 1 条）。
>
> **规则共 9 条**：编号从「第 0 条」到「第 8 条」（第 0 条是开工前的预备动作，也算一条）。

---

## 第 0 条　每次开工前，先做这三件事

1. **读这个文件**（`AGENTS.md`）+ 读 `PRD.md` / `TECH_DESIGN.md` 里和今天相关的章节。
2. **复述一遍今天要做什么**，让我点头确认后再动手。**我不点头，不许开始写。**
3. 如果发现我的要求**和这个文件里的规则冲突**，**停下来告诉我**，
   说清「你要的 A 和规则第 N 条 B 撞了」，等我说了算。

---

## 第 1 条　当天只做当天任务，不越界

> ⭐ **这是 Day 1 写入的规则，原样保留，不许删改。**

- **只做我今天明确说要做的事。** 我说了「进入下一板块」或者「开始下一步」，
  你才可以往下走；我没说，就停在这一步。
- **不许顺手多做**。看到别的地方有问题、有更好的写法、想顺便优化一下——
  **先说出来问我要不要做**，不许自己动手。
- **今天说「不做」的事，就是不做。** 哪怕你只花一分钟、哪怕你觉得零成本、
  哪怕顺手就能改好——**不做**。清单上写了「今日不做」的，一个字都别碰。
- **做完就停，等我确认。** 不要自动开始下一件事、不要自己制定下一步计划然后直接执行。
- **范围变了要先说。** 如果我中途改主意加东西，你要提醒我「这超出了原计划」，
  让它成为一个我明确知道的决定，而不是悄悄混进去。

**这条防的是什么**：AI 太积极，一口气干完十件事，其中八件不是我要的 ——
我既没法检查，也搞不清哪一步出的问题，还容易把已经定好的东西改坏。

---

## 第 2 条　不许悄悄改产品范围（不许「顺手加功能」）

- **`PRD.md` 定下的东西不许动**，特别是这几条（它们是刻意的设计，不是遗漏）：
  - **来源上限 5 个**，加不了第 6 个 —— 不是 bug，是设计。
  - **手动粘贴，不做自动抓取** —— 纯静态网页受浏览器同源策略（CORS）限制，技术上做不到。
  - **条目就三个字段**（来源 + 标题/链接 + 我的一句话），**不加星标、不加已读状态**。
  - **来源卡片按加入顺序固定排**，**不按「最久没看」排序**（位置天天跳会找不到）。
  - **「我的一句话」可以留空**，不许改成必填。
  - **阈值 7 天写在代码一处常量，不做设置页**。
  - **不做**：账号注册、社交、多人使用、提醒推送、标签体系、全文搜索、
    网页快照、AI 摘要、手机 App / 小程序。
- 想加任何新功能/新字段/新页面，**必须先问我**，我同意了再记进 `PRD.md`，然后才写代码。
- 觉得哪条设计不合理想改，**说出来跟我讨论**，不许直接改掉。

**这条防的是什么**：AI 觉得「这样更好」，顺手把定好的东西改了，
等我发现时已经面目全非，前面的文档全白写了。

---

## 第 3 条　改完必须证明真的能用

> ⭐ **这是我（Jimmy）在 Day 6 追加的规则。**

- **说「做好了」之前，必须真的跑过一次。** 不许只写完代码就交差。
- **怎么算「跑过」**（针对本项目）：
  1. 用 **Edge 或 Chrome 双击打开**改动过的页面（`index.html` / `source.html`）；
  2. **真的点一遍这次改动涉及的功能**（不是看代码觉得对，是动手点）；
  3. 涉及数据的，**关掉浏览器再打开一次**，确认数据还在；
  4. 涉及导出的，**确认文件真的落进了下载文件夹**、用记事本能打开。
- **如果改的是不经过页面的文件**（比如只改了 `assets/config.js` 的常量、
  或只改了 `assets/rules.js` 的算法），**不能只打开页面看一眼就算跑过**——
  你要**构造一个能触发那段逻辑的操作**：改常量的，就在页面上把那个功能走一遍
  （比如把阈值临时调成 1 天，看首页那句话有没有跟着变），
  验证完**把临时改的常量改回去**，并说明改回去了。
  **跑不了就老实说跑不了**，不要拿「双击打开了页面」冒充验证过。
- **报告时要交出证据**：
  - 哪一次操作（点了什么按钮 / 打开了哪个文件）；
  - **看到的结果是什么**（截个图，或者把实际输出贴出来）；
  - 有没有报错。**有报错就照实说，不许说「应该没问题」。**
- **不许用「理论上」「应该可以」「看起来没问题」交差。**
  没跑过就说没跑过，比让我以为跑过要好得多。
- **跑不了的（比如需要我手动操作、需要断网测试）**，明确告诉我
  「这一步我验证不了，需要你自己双击试一下」，并写清让我怎么试。
- **说明**：为了验证而临时改动某个值（比如把阈值调成 1 天试一下），
  **不算违反第 1 条「不许顺手改」**——但它必须满足两个条件：
  ① **只为验证、验完立刻改回原样**；② **报告时明确说明「我临时改过 X，已经改回来了」**。
  验完忘了改回去，比不验证更糟。

**这条防的是什么**：AI 说「改好了」，我打开一看是坏的；
或者它自己也没试过，我成了第一个发现 bug 的人 —— 而它本来有机会先发现。

---

## 第 4 条　技术硬规矩（来自 `TECH_DESIGN.md` 第 5 节，Day 5 实测得出）

这几条是**不可让步**的，写错任何一条都会让网站用不了或者用起来很糟：

| # | 规矩 | 为什么 |
| --- | --- | --- |
| 1 | **禁止 `<script type="module">`**，所有 JS 用传统 `<script src="...">` 按顺序加载 | **实验 3 实测**：`file://` 下模块脚本会被浏览器直接拦掉，页面白屏 |
| 2 | **不许出现任何在线资源**：不引 CDN、不用在线字体、不用在线图标库（图标用内联 SVG 或文字） | 断网必须能用（硬约束 C1）；而且这条有检查项 TC-03 |
| 3 | **不引入构建步骤**：不放 `package.json`、不生成 `node_modules`、不要「先打包才能运行」 | 双击就要能用，改完刷新就该生效 |
| 4 | **不引入任何框架/库**（React、Vue、jQuery、Tailwind、Bootstrap 都不行） | 要么需要构建链，要么需要联网，两样都破坏硬约束 |
| 5 | **用户输入只用 `textContent` 塞进页面**，不许拼 `innerHTML` | 我贴的标题里可能有 `<` `>`，拼 HTML 会让页面错乱 |
| 6 | **数据读写只走 `assets/store.js`**，其他文件里禁止直接出现 `localStorage` | 以后换云端只改这一个文件 |
| 7 | **常量只在 `assets/config.js`**：`7`、`5`、存储键名、格式版本号不许散落在别处 | 以后改阈值只改一行；有检查项 TC-08 |
| 8 | **时间统一存 ISO 8601 带时区**（`2026-09-20T21:12:18+08:00`），只在显示时变成「今天 / 3 天前」 | 存储格式统一，显示才灵活 |
| 9 | **删除类操作统一走 `confirmDanger()` 封装**，提示里必须写明「会连带删掉多少条记录」 | 数据删了找不回来 |
| 10 | **「X 天前」按自然日算**（先抹掉时分秒再相减），不许用「现在减记录时间除以 24 小时」 | 见 `TECH_DESIGN.md` 5.1：否则 18 号 23:50 的记录到 20 号 00:10 会算成 1 天，跟直觉对不上 |
| 11 | **文件名用小写加连字符、不带空格** | 避免本地路径出问题 |

**文件分工**（谁负责什么，不许混）：

```
index.html              首页（页面骨架）
source.html             某个来源的详情页
assets/style.css        所有视觉
assets/config.js        所有常量（7 / 5 / 键名 / 版本号）
assets/store.js         所有数据读写（唯一碰 localStorage 的文件）
assets/rules.js         所有"算"的逻辑（X 天前、该看哪个来源、顶部那句话）
assets/ui.js            所有"画"和"收集输入"（弹窗骨架也在这里）
```

一句话：**界面只负责「画」和「收集输入」，算数交给 `rules.js`，存取交给 `store.js`。**
三件事不许混在一起。

---

## 第 5 条　动我之前，先说清后果

- **可能丢数据的操作**（删除、导入覆盖、清空）：先说清楚**会丢什么、能不能撤销**，
  等我明确同意再动手。
- **改文件名 / 改目录结构**：先说清**会影响哪些文件**，再说怎么做。
- **改已定稿的文档**（`PRD.md` / `TECH_DESIGN.md`）：**只许追加和修正，不许推倒重写**；
  改完要写清「改了第几节、为什么改」，并在文末记一笔。
- **删文件**：一律先问。
- **不确定能不能撤销的，一律当成不能撤销来对待。**

**这条防的是什么**：有些操作看着小，实际不可逆。我又不是专业出身，
你不提醒我，我根本不知道刚才那一下把什么弄没了。

---

## 第 6 条　讲人话，把过程说清楚

- **称呼我 Jimmy。** 用中文。
- **别只给结论**：说清每一步在做什么、为什么这么做。
- **专业词要翻译**（PATH、权限、依赖、环境变量、CORS、打包……），
  出现一个就用一句大白话解释一下，**不要默认我懂**。
- **给命令要交代后果**：这条命令干什么、会改什么、能不能撤销。有不可逆风险必须先提醒。
- **给网址要能整段复制**：网址独占一行放在代码块里，
  并且要写明「**先按 `Ctrl + L` 选中地址栏，再粘贴、再回车**」。
- **只给一条主路径**，编好 1/2/3 步；**不要并列两个方案让我选**。
- **每次都要指出下一步**可以做什么，不要只说「已完成」。

---

## 第 7 条　收尾动作（每天固定）

1. **列出改动文件清单**，并说明**每个文件属于哪一天的任务**。
2. **本地提交**：标题 `Day N｜一句话`，正文两行分别写「改了什么」「加了什么」。
3. **推送交给我在本机终端跑**（你在沙箱里推不了，不用试）。
   推送命令固定写成这样（先直连、失败自动走本机中转口）：
   ```
   cd /d C:\Users\33386\WorkBuddy\2026-09-17-16-01-57 && git push || git -c http.proxy=http://127.0.0.1:1710 push
   ```
4. **推完停下等我确认**，不要自己接着做下一件事。
5. **判断「推没推」只认远程 SHA**（`git ls-remote origin main`），
   **不许认截图、不许认窗口里的文字** —— 那可能是一小时前的残留输出。

---

## 第 8 条　不许动的东西

- **`.env` 绝不上传**（已被 `.gitignore` 忽略）。
  提交前照例 `git check-ignore -v .env` 验一次；
  **如果发现它真的混进了暂存区，立刻停下来告诉我**，用
  `git rm --cached .env` 把它从暂存区摘掉（**这条命令只影响 git 的暂存区，
  不会删掉你磁盘上的文件**），确认摘干净了再继续提交。
- **`.workbuddy/` 目录不上传**（已被忽略）。
- **`AGENTS.md` 这个文件本身**：你要改它，**先问我**。
  它是我和你的约定，不是你想优化就优化的东西。
- **上面第 7 条的推送命令里那个路径是我这台电脑的固定路径**。
  换机器或换目录时，那个路径要跟着换 —— **拿不准就先问我**，不要照抄。

---

## 附：这个文件是怎么来的

| 时间 | 发生了什么 |
| --- | --- |
| Day 1 | 按清单原计划应写入规则文件，但实际上**没有留存**（项目里查不到） |
| Day 6（2026-09-21） | 检查发现文件不存在 → 走**卡住降级方案** → 重建第 1–8 条，保留「当天只做当天任务」，**追加第 3 条「改完必须证明真的能用」** |

**规则清单速查**：

| 条 | 一句话 | 来源 |
| --- | --- | --- |
| 0 | 开工先读规则、复述任务、等我点头 | Day 6 建立 |
| 1 | **当天只做当天任务，没让做的就不做** | **Day 1（保留）** |
| 2 | 不许悄悄改产品范围 | Day 6 建立 |
| 3 | **改完必须证明真的能用** | **Day 6 追加（Jimmy 的规则）** |
| 4 | 技术硬规矩 11 条（禁模块 / 禁 CDN / 禁框架 / 禁构建…） | Day 5 实测 |
| 5 | 动我之前先说清后果 | Day 6 建立 |
| 6 | 讲人话，把过程说清楚 | Day 6 建立 |
| 7 | 收尾动作：清单 → 提交 → 我推送 → 停下 | 每日约定 |
| 8 | 不许动的东西（`.env` / `AGENTS.md` 本身） | Day 6 建立 |
