# ============================================================
# .gitignore —— Git 的「忽略名单」
#
# 写在这个文件里的文件和文件夹，Git 会自动当作「不存在」，
# 既不会被提交，也不会被推送到 GitHub。
# 它保护的是：「只应该留在你自己电脑上」的东西。
# ============================================================

# --- 环境变量文件（最重要的一条！绝对不能上传）---------------
# .env 里以后会放数据库密码、API 密钥这类敏感信息
.env
.env.local
.env.*.local

# --- 依赖包（体积巨大，而且可以用命令重新装回来）-------------
node_modules/
__pycache__/
venv/
.venv/

# --- 操作系统 / 编辑器产生的杂项文件 --------------------------
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# --- 日志和临时文件 ------------------------------------------
*.log
logs/
tmp/
dist/
build/

# --- 工具自己的数据目录（和网站本身无关）---------------------
.workbuddy/
