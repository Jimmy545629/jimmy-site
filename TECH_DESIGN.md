# TECH_DESIGN.md — 「来源看板」技术方案

| 项目 | 内容 |
| --- | --- |
| 产品名 | **来源看板** |
| 所属项目 | `jimmy-site`（https://github.com/Jimmy545629/jimmy-site） |
| 文档版本 | v1.1（含 AI 自检后修订） |
| 日期 | 2026-09-20 |
| 作者 | Jimmy |
| 状态 | 已定稿，Day 7 按本文档开发 |
| 上游文档 | `PRD.md`（Day 4 产品需求文档，本文档的所有技术选择都以它的约束为依据） |
| 下游文档 | `AGENTS.md`（Day 6，给 AI 的施工规则）；Day 7 开发第一个能跑的 MVP |

> **这份文档要解决什么**：`PRD.md` 回答的是「做成什么样」，本文档回答的是
> 「**用什么做、东西放哪、怎么保证那三条硬约束真的成立**」。
>
> `PRD.md` 第 8.1 节把 **4 个待定项 T1–T4** 明确交给 Day 5 决定：
> T1 数据怎么存 / T2 页面拆几个文件 / T3 数据文件格式 / T4 阈值写在哪。
> `PRD.md` 第 8.2 节的 R1 还额外要求：**「数据能直接拷走」这一条必须先用小样验证一遍**。
> —— 验证过程与结果全部记录在第 6 节，不是推测，是真跑过的。

---

## 1. 三条硬约束怎么落地

`PRD.md` 第 2.3 节的三条硬约束不可让步，它们直接决定了本文档后面所有的选择：

| 约束 | 技术上的对应决定 | 怎么证明它成立 |
| --- | --- | --- |
| **C1　不依赖任何服务器**，断网也能用 | 纯静态网页（HTML/CSS/JS 三样），双击打开；**零第三方依赖**，不引用任何在线资源 | 实验 1a：`file://` 页面本地存储可用；实验 3：本地 CSS/JS 都能加载。**外加一条硬规矩：全项目不出现任何 `http://` / `https://` 的资源引用**（见 TC-03） |
| **C2　数据必须能变成一个我能直接打开、能直接拷走的文件** | 日常数据存浏览器本地存储（localStorage）；**「导出」按钮一键生成一个 `.json` 文件**下载到磁盘 | 实验 1b：**真的点了一次导出，文件真的生成到了下载文件夹**（409 字节，用记事本可读，见第 6 节） |
| **C3　来源数量硬性上限 5 个** | 上限值写进 `assets/config.js` 一处常量，由它拦住第 6 个来源 | 全项目只有 `config.js` 出现这个数字（TC-08） |

对应的 PRD 验收标准（AC-13 / AC-14 / AC-15）也一并落在上面的方案里，第 7 节有逐条的技术自查表。

---

## 2. 选型总表（一眼看完）

| # | 决定项 | 候选 | **决定** | 一句话理由 |
| --- | --- | --- | --- | --- |
| 1 | 运行形态（T2 的一半） | 双击本地文件 / 本地起服务 / 直接托管上网 | **双击本地文件（`file://`）** | 双击就用，不装东西、不敲命令——这正是 C1 和 PRD 8.1 提醒的意思 |
| 2 | 语言与依赖 | 原生三件套 / React·Vue + 构建工具 / CDN 引入的库 | **原生 HTML + CSS + JS，零第三方依赖** | 引入框架要 Node 构建链，引入 CDN 要联网，两样都破坏 C1 |
| 3 | JS 写法 | ES 模块 `type="module"` / 传统 `<script src>` | **传统 `<script src>`** | **实验 3 实测：`file://` 下模块脚本被浏览器拦住**（必须避开） |
| 4 | 数据存放（T1） | localStorage / IndexedDB / File System Access API / cookie | **localStorage** | 容量够（几 MB）、免安装、断电重启数据还在（实验 1a + 实验 2 已验证） |
| 5 | 「变成文件」的方式 | 导出 JSON / 导出 CSV / 复制文本 | **一键导出 `.json` 文件 + 导入还原** | 双击能打开、人能读懂、结构确定，符合 F9-2（实验 1b 已验证） |
| 6 | 页面拆分（T2 的一半） | 1 个 HTML 全包 / 多个 HTML + 共享资源 | **2 个 HTML（`index.html`、`source.html`）+ 共享 `assets/`** | 每个文件职责单一；弹窗不单独成文件（PRD 6.3 里它本来就是弹窗） |
| 7 | 阈值和上限写哪（T4） | 散落在各文件 / 集中一处 | **`assets/config.js` 一处常量** | 以后想改 7 天或 5 个，只改一行 |
| 8 | 部署 | Day 7 就上网 / 先不上网 | **Day 7 不上网，本地双击用**；上网留到第 4 周（Day 22+） | 产品是给自己用的，先跑起来；上网要不要另做决定，见风险 R2/R3 |

---

## 3. 每个决定是怎么来的

### 3.1 运行形态：双击本地 HTML 文件　【T2】

| 候选 | 好处 | 为什么不选 |
| --- | --- | --- |
| **双击本地文件（选定）** | 双击就用；不装 Python、不装插件、不记命令；断网照常 | — |
| 本地起服务（`python -m http.server`、VS Code Live Server） | 有些浏览器功能只在服务下才开放（比如模块脚本） | 要装东西、每次开发前先"启动服务"；而且 PRD 第 8.1 节的提醒说得很清楚：**需要起服务的方案不满足 C1**，因为那又变成"要会部署"了 |
| Day 7 就托管到网上 | 手机也能打开 | 国内访问 GitHub 不稳（Day 3 已实际踩到超时）；上网之后要处理缓存、跨域这些新问题，属于第 4 周的内容 |

### 3.2 语言与依赖：原生三件套，零第三方依赖

**决定**：只用 HTML、CSS、原生 JavaScript；不引入任何框架、任何库、任何在线资源。

**为什么不用 React / Vue**：它们需要"构建工具链"——要用 Node.js 把源码编译打包成浏览器能懂的文件。等于每次改一行代码都要先跑一次打包命令，出错的环节从 1 个变成 5 个。而我们的需求是 2 个页面、10 个功能（`PRD.md` 第 5.1 节），原生 JS 完全够。

**为什么不用 CDN 引入的库**（比如 `<script src="https://cdn.../vue.js">`）：

> 因为 `PRD.md` 约束 C1 要求**断网也能用**。只要页面里出现一个 `https://` 开头的资源，断网时那一行就加载失败，页面就残了。
> 这一条后来变成了工程规矩：**全项目不允许出现任何在线资源引用**（TC-03 会逐行检查）。

**样式**：手写一个 `style.css`，用 CSS 变量统一管颜色。不用 Tailwind/Bootstrap——它们要么同样来自 CDN，要么需要构建步骤。

### 3.3 数据存放：localStorage + 导出文件　【T1　最关键的一项】

T1 是 PRD 里唯一"可能做不成"的地方（第 8.2 节 R1）。逐个对比：

| 候选 | 能存多少 | 断网可用 | 能不能变成"我能拷走的文件" | 复杂度 |
| --- | --- | --- | --- | --- |
| **localStorage（选定）** | 每个浏览器约 5MB（我们几十 KB 都用不到） | ✅ | 原生不行，**但配合"导出"就可以**（实验 1b 已证明） | 最简单：一行 `localStorage.setItem` |
| IndexedDB | 几乎无限 | ✅ | 同样要自己写导出 | 异步、回调多，代码量是 localStorage 的 3–5 倍，本项目用不上 |
| File System Access API | 直接读写磁盘上真实的文件 | ✅ | ✅✅ 直接就是一个文件，最贴合 C2 | **本机实验证明可用**（实验 1a：`showSaveFilePicker` 存在），但放弃，理由见下 |
| cookie | 约 4KB | ✅ | ❌ | 容量根本不够，排除 |

> **为什么放弃了看起来最贴合 C2 的 File System Access API？**（实验证明它在你机器上是可用的，所以这是一个"主动放弃"而不是"做不到"）
>
> 1. 只有 Chromium 系浏览器支持（Chrome / Edge 可以，Firefox / Safari 不行），手机上更不行；
> 2. **每次保存都会弹一次系统对话框问"存哪"**——而我们最高频的操作是"贴一条"（`PRD.md` 场景 S1），一次点击变成两次点击加一次选文件夹，正好破坏了 PRD 里那句"我要 10 秒记完"；
> 3. 文件被移动或改名后，页面对它的"引用"就失效了，要重新选一次，反而更容易出莫名的问题。
>
> 结论：**它作为"以后可选的小增强"记在 8.3 节，但不是本期主路径。**

**最终方案（两层）**：

| 层 | 干什么 | 具体做法 |
| --- | --- | --- |
| 日常读写 | 每次增删改后立刻存 | `localStorage` 一个键 `sourceboard.data.v1`，整份数据存成一个 JSON 字符串 |
| 变成文件 | 一键把数据落成磁盘上的文件 | 「导出」按钮：把数据转成 JSON → 生成 Blob → 触发下载，文件名 `sourceboard-YYYY-MM-DD.json` |

**为什么用"一个键存整份数据"**：读写只有"整份读出、整份写回"两种动作，不会出现"改了一半、另一半没保存"的中间状态。数据量小（几十 KB），性能上毫无压力。

**已知代价（必须诚实写下来）**：数据存在"这个浏览器 + 这个用户"里。

- 换浏览器（Chrome 换 Edge）→ 数据不会跟过去 → **用导出的 JSON 导入一次**；
- 清理浏览器数据 / 重装系统 → 数据会丢 → **导出文件就是唯一可靠的备份**。

所以规矩是：**每周导出一次；做了重要改动（比如一口气补看了三个来源）之后立刻导出一次。**

**为 Day 15 之后留的缝**：所有数据读写只允许经过 `assets/store.js` 这一个文件，别的地方一律不许碰 localStorage。将来要换成云端数据库，只改这一个文件。

### 3.4 数据文件格式　【T3】

**决定**：`.json`（UTF-8 编码），字段名用英文（代码友好），值可以是中文。

```json
{
  "schemaVersion": 1,
  "app": "sourceboard",
  "exportedAt": "2026-09-20T21:12:18+08:00",
  "sources": [
    { "id": "s-mf3k2a1b", "name": "掘金", "url": "https://juejin.cn", "createdAt": "2026-09-18T10:00:00+08:00" }
  ],
  "items": [
    {
      "id": "i-9d2k0z7x",
      "sourceId": "s-mf3k2a1b",
      "title": "看到的一篇文章标题",
      "url": "https://juejin.cn/post/xxxx",
      "note": "为什么值得记",
      "recordedAt": "2026-09-19T10:00:00+08:00",
      "savedAt": "2026-09-19T10:03:11+08:00"
    }
  ]
}
```

字段与 `PRD.md` 第 4 节的数据模型一一对应，另外加了两处纯技术字段：

| 字段 | 对应 PRD | 说明 |
| --- | --- | --- |
| `schemaVersion` | — | 格式版本号。以后结构变了，靠它判断能不能读 |
| `exportedAt` | — | 导出时间，方便我知道这份备份是哪天的 |
| `sources[]` / `items[]` | 4.1 / 4.2 | 来源列表、条目列表 |
| `savedAt` | — | **技术字段**：「这条是什么时候被贴进来的」。产品和界面上不显示它，只用来排序（见 5.3） |

**文件名**：`sourceboard-2026-09-20.json`（带日期，一眼看出是哪天的备份，多次导出也不会互相覆盖）。

**时间格式**：一律用「`2026-09-20T21:12:18+08:00`」这种带时区的写法（ISO 8601），人看得懂、程序也不会算错。

**为什么不用 CSV**：来源和条目是两层关系（一个来源下面挂多条），CSV 只能摊平成一张表，导出时要拆、导入时要拼，很容易错位。JSON 天生带层级，一份文件装下全部。

**导入的规矩**（F10）：先检查 `schemaVersion` 和两个数组是否存在，读不懂就明确报错而不是默默把数据清掉；**并且导入前先自动导出一份当前数据当备份**——导入是唯一会覆盖数据的操作，先备份比单纯弹个"确定吗"可靠。

### 3.5 页面与文件拆分　【T2】

**决定**：2 个 HTML 文件 + 1 个 `assets/` 文件夹。

```
jimmy-site/
├── index.html          ← P1 首页：顶部提示条 + 来源区 + 时间线区
├── source.html         ← P2 来源详情页：?id=来源id
├── assets/
│   ├── config.js       ← 全部常量（7 天、5 个、存储键名、格式版本）
│   ├── store.js        ← 数据的唯一出入口（读、写、导出、导入）
│   ├── rules.js        ← 纯计算：最后记录、天数、该看哪个来源、排序
│   ├── ui.js           ← 画页面、绑事件、弹窗（两个页面共用）
│   └── style.css       ← 全部样式
├── research.md / PRD.md / TECH_DESIGN.md / AGENTS.md
└── README.md
```

| 文件 | 职责 | 谁调用它 |
| --- | --- | --- |
| `config.js` | 只放常量，不含逻辑 | 所有文件 |
| `store.js` | 把 localStorage 包起来；导出/导入 | `ui.js` |
| `rules.js` | 不碰数据、不碰界面，只做计算（纯函数，最好测） | `ui.js` |
| `ui.js` | 把算好的结果画到页面上；处理点击/保存 | 两个 HTML |
| `style.css` | 视觉 | 两个 HTML |

**为什么不是"3 个页面各写一个 HTML"**：`PRD.md` 第 6.3 节写明 P3 是**弹窗**，不是独立页面，所以只有 2 个 HTML 文件。

**为什么不是"全部塞进 1 个 HTML"**：首页和详情页要各自能被双击打开（PRD 6.1 / 6.2 都写了跳转关系），拆成两个文件后每个文件都短、都能单独打开；更重要的是——**一个文件越来越长以后，"改坏一处影响另一处"的风险会急剧上升**，这是 Day 8 之后要继续改页面时最容易踩的坑。

**弹窗放在哪**：弹窗的 HTML 骨架由 `ui.js` 生成，**两个页面共用同一段代码**（不然要在两个 HTML 里各抄一份，以后改表单得记着改两处，迟早漏一处）。

### 3.6 阈值和上限写在哪　【T4】

**决定**：集中在 `assets/config.js`，只此一处。

```js
window.SB_CONFIG = {
  STALE_DAYS: 7,                        // 「多久没记录算该看了」——PRD F2
  MAX_SOURCES: 5,                       // 来源上限——PRD 硬约束 C3
  STORAGE_KEY: 'sourceboard.data.v1',   // 存哪
  SCHEMA_VERSION: 1                     // 数据文件格式版本
};
```

页面里所有地方要用到这两个数字，都写 `SB_CONFIG.STALE_DAYS`，不许再写一个 `7`。以后觉得 7 天太频繁，改这一行，全站跟着变（TC-08 会检查）。

---

## 4. 页面之间的数据流

```
  用户点「保存」
        │
        ▼
    ui.js  →  拼出一条 Item  →  store.js.save()
        │                              │
        │                              ▼
        │                    localStorage（sourceboard.data.v1）
        ▼
   rules.js 重算：最后记录 / X 天前 / 该看了 / 顶部那句话
        │
        ▼
    ui.js 重新画一遍页面（不需要刷新浏览器）
```

一句话：**界面只负责"画"和"收集输入"，算数交给 `rules.js`，存取交给 `store.js`。** 三件事互不混在一起，将来哪一层出问题都好定位。

---

## 5. 写代码时必须遵守的约定

这几条是 Day 7（以及以后每一天）写代码时的硬规矩，会同样抄进 Day 6 的 `AGENTS.md`。

1. **不用 ES 模块**：所有 JS 用传统 `<script src="...">` 按顺序加载，禁止 `<script type="module">`。
   *理由：实验 3 实测，`file://` 下模块脚本会被浏览器直接拦掉（CORS）。*
2. **不许出现任何在线资源**：不引 CDN、不用在线字体、不用在线图标库。图标用内联 SVG 或文字。
   *理由：C1 要求断网可用；AC-16 / AC-17 要求页面上不出现第三方服务入口。*
3. **不引入构建步骤**：仓库里不放任何"必须先打包才能运行"的东西，不生成 `node_modules`。改完代码刷新浏览器就该生效。
4. **用户输入只用 `textContent` 塞进页面**，不拼 `innerHTML`。
   *理由：我贴的标题里万一有 `<` `>` 这类符号，拼 HTML 会让页面错乱。*
5. **数据读写只走 `store.js`**，其他文件里禁止直接出现 `localStorage`。
   *理由：Day 15 之后可能要换云端，收口一个文件才好换。*
6. **时间统一存 ISO 8601 带时区**（`2026-09-20T21:12:18+08:00`），只在显示时才变成"今天 / 3 天前"。
7. **常量只在 `config.js`**：`7`、`5`、存储键名、格式版本号都不许散落在别处。
8. **删除类操作统一走一个 `confirmDanger()` 封装**，把「会连带删掉多少条」写进提示文字（PRD F7 / F8 / F10 都要求二次确认）。
9. **文件名用小写加连字符、不带空格**，避免本地路径出问题。

### 5.1 「X 天前」怎么算（容易出错，单独写清楚）

PRD 第 4.1 节要的是**自然日天数**（今天 20 号、最后记录 18 号 = 2 天），不是"过了多少小时"。所以两边都先抹掉时分秒，只留日期再相减：

```js
function daysSince(fromISO, now) {
  var a = new Date(fromISO);
  var d1 = new Date(a.getFullYear(), a.getMonth(), a.getDate());   // 那一天的 0 点
  var d2 = new Date(now.getFullYear(), now.getMonth(), now.getDate()); // 今天的 0 点
  return Math.round((d2 - d1) / 86400000);                          // 86400000 = 一天的毫秒数
}
```

这样「18 号 23:50 记录」和「20 号 00:10 打开」算出来是 2 天，符合直觉。
**不许直接用"现在时间减去记录时间再除以 24 小时"**——那样会算出 1.01 天这种结果，再取整就变成 1 天，和我实际感觉对不上。

### 5.2 「该看了」怎么判断（PRD F2 的四条边界）

| 情况 | 判断 |
| --- | --- |
| 从未记录过 | 算「该看了」，卡片显示「还没有记录过」（**不能显示"0 天前"**，PRD AC-02） |
| `daysSince >= 7` | 算「该看了」 |
| 全部来源都在 7 天内 | 顶部改成「所有来源都在 7 天内记录过，暂时不用补看」，**不点名**（PRD AC-04） |
| 多个来源都超标 | 只点名天数最多的那个；天数相同就取加入顺序更早的 |

### 5.3 排序规则

| 位置 | 规则 |
| --- | --- |
| 来源卡片（首页） | 按 `createdAt` **升序**（加入顺序固定，位置不跳动——PRD F1 明确要求） |
| 时间线 | 先按 `recordedAt` **从新到旧**；**时间相同时按 `savedAt` 从新到旧** |
| 某来源下的条目 | 同上 |

> 为什么要第二个排序键：因为「记录时间」是**允许我手改**的（PRD 4.4）。改完之后很可能出现两条时间一模一样，这时候只按一个键排，顺序就会飘（PRD AC-07 要求"同一天的多条按保存先后，不能乱"）。

---

## 6. 验证记录（2026-09-20 实测，不是推测）

环境和方式：本机 **Microsoft Edge 无头模式**（`--headless=new`），在临时目录里跑了 4 个一次性测试页，截图/文件作为证据。测试页放在当天工作区的 `.tmp/` 里，**不进仓库**。

### 6.1 实验 1a：能力探测（页面能干什么）

| 探测项 | 结果 |
| --- | --- |
| `isSecureContext` | `true`（`file://` 页面算是"安全来源"，所以很多高级接口能用） |
| `location.origin` | `file://`（**所有本地 HTML 文件共享同一个来源**，这是数据能跨文件共享的原因） |
| `localStorage` 写入 + 读回 | ✅ 成功（写入 `hello-2026-09-20`，读回来一致） |
| `showSaveFilePicker` / `showOpenFilePicker` | 都存在（可以用，但按 3.3 的理由主动放弃） |
| `Blob` / `URL.createObjectURL` | 都存在 |
| `<a>` 的 `download` 属性 | 支持 |

### 6.2 实验 1b：**真的点一次"导出"，文件能不能落盘**（这就是 PRD R1 要求验证的那一条）

- 操作：页面里生成 JSON → 触发下载。
- 结果：**下载文件夹里真的出现了 `sourceboard-export-test.json`，409 字节。**
- 打开看的内容（原文，中文没乱码）：

```json
{
  "schemaVersion": 1,
  "exportedAt": "2026-09-20T13:12:18.398Z",
  "sources": [{ "id": "s1", "name": "掘金", "url": "https://juejin.cn", "createdAt": "2026-09-18T10:00:00+08:00" }],
  "items": [{ "id": "i1", "sourceId": "s1", "title": "测试条目", "url": "", "note": "", "recordedAt": "2026-09-19T10:00:00+08:00" }]
}
```

**结论：约束 C2 成立，AC-14 的技术前提成立。** PRD 第 8.2 节的 R1（"数据能直接拷走可能有限制"）**风险解除**——不用退回到"一键导出 + 手动每周存"的降级方案，因为"一键导出"这一条本身就实测通过了。

### 6.3 实验 2：关掉浏览器、换一个路径再打开，数据还在不在

- 操作：第一个页面（`.tmp/exp/exp1.html`）写入一个值 → **关掉浏览器进程** → 用同一份浏览器配置打开**另一个路径下**的页面（`.tmp/exp/sub/exp2.html`）读取。
- 结果：**读到了**——报表原文：「读到 exp1 写入的值 = hello-2026-09-20」「结论 = 【通】数据跨文件、跨浏览器重启都还在」。

**结论：AC-13 的技术前提成立**（关掉浏览器再打开，内容还在），并且**两个 HTML 文件能共享同一份数据**——这是 3.5 节"拆成两个文件"能成立的原因。

### 6.4 实验 3：本地网页加载外部 CSS / JS 的限制

| 写法 | 结果 |
| --- | --- |
| `<link rel="stylesheet" href="assets/style.css">` | **【通】** 样式生效 |
| `<script src="assets/app.js">`（传统脚本） | **【通】** 执行了 |
| `<script type="module" src="assets/mod.js">` | **【不通】被浏览器拦住** |

**结论：这就是本文档第 5 节第 1 条那条规矩的来源**——`file://` 下模块脚本被 CORS 拦掉，所以全部 JS 必须用传统 `<script src>` 按顺序加载。这一条如果在 Day 7 之后才发现，会白折腾半天。

### 6.5 已经证明的 / 还没证明的（诚实清单）

| 事项 | 状态 |
| --- | --- |
| 本地页面能用本地存储，且浏览器重启后不丢 | ✅ 已证明（6.1、6.3） |
| 页面能生成并保存一个可读的 JSON 文件 | ✅ 已证明（6.2） |
| 本地页面能加载外部 CSS 和传统 JS | ✅ 已证明（6.4） |
| 模块脚本不能在本地页面用 | ✅ 已证明（6.4） |
| 真实的「点按钮 → 文件进下载文件夹 → 双击用记事本打开」全流程 | ⏳ **Day 7 做出来之后必须由我亲手跑一次**（6.2 是无头模式跑的，流程等价，但要亲眼确认一次） |
| 清理浏览器数据后会丢 | ⏳ 未测（后果已知，靠"每周导出"应对） |
| 手机浏览器打开的效果 | ⏳ 未测，按 PRD 只要求"能用即可" |

---

## 7. 技术验收标准（Day 7 拿它逐条自查）

> 这些是**技术层面**的验收标准，和 `PRD.md` 第 7 节的 AC 是两套：AC 管"产品对不对"，TC 管"技术做法有没有走偏"。

| 编号 | 技术验收标准 | 对应 |
| --- | --- | --- |
| **TC-01** | 双击 `index.html` 就能用；不需要装任何软件、不需要敲任何命令、不需要起服务 | C1、T2 |
| **TC-02** | **断网**（关掉 Wi-Fi）后打开页面，全部功能照常可用 | C1、AC-15 |
| **TC-03** | 页面和脚本里**不出现任何 `http://` / `https://` 的资源引用**（只有我自己填进去的链接地址不算） | C1、AC-17 |
| **TC-04** | 记几条内容 → 关掉浏览器 → 重新打开，内容一条不少 | AC-13 |
| **TC-05** | 点「导出」能生成 `sourceboard-YYYY-MM-DD.json`，双击用记事本能打开、能读懂 | AC-14、F9-2 |
| **TC-06** | 把导出的文件拷到别的文件夹/U 盘，它仍然是一份完整数据（内容自包含） | F9-3 |
| **TC-07** | 「导入」能用导出的文件还原数据；导入前有二次确认，且**先自动备份当前数据** | F10 |
| **TC-08** | 全项目只有 `config.js` 里出现阈值 `7` 和上限 `5`（脚本逐文件搜一遍确认） | T4 |
| **TC-09** | 除 `store.js` 外，没有文件直接读写 `localStorage` | 3.3 |
| **TC-10** | 全项目没有 `type="module"`、没有 CDN、没有 `node_modules`、没有构建步骤 | 3.2、第 5 节第 1 条 |
| **TC-11** | `index.html` 和 `source.html` 都能被单独双击打开并正常显示 | 3.5 |
| **TC-12** | Chrome 和 Edge 里都能正常打开（手机浏览器"能用即可"，不做额外要求） | PRD 3.2 |

---

## 8. 风险与待定项

### 8.1 已知风险

| 编号 | 风险 | 影响 | 应对 |
| --- | --- | --- | --- |
| **R1** | 数据只存在"这个浏览器"里，清理浏览器数据或换浏览器会丢 | 可能丢掉记录 | **每周导出一次**；做重要补看之后立刻导出一次；导入时先自动备份 |
| **R2** | **约束 C1（不依赖服务器）和课程 Day 15 起的"云端数据库"存在方向冲突** | 到 Day 15 必须重新做一次决策 | 本期先按 C1 做（PRD 8.3 已把数据库列为"后续版本"）。到 Day 15 时二选一：① 云端只当**同步副本**，本地仍是主数据；② 整体迁到云端，并**明确改掉 PRD 的 C1**。技术上已经准备好了——数据读写都收口在 `store.js`，改一个文件就能换 |
| **R3** | 第 4 周部署到网上（https）之后，`file://` 和 `https://` 是**两个不同的来源**，本地老数据不会自动跟过去 | 上线后看到的是空的 | 上线后用导出的 JSON 导入一次即可（这也是 C2 的价值） |
| **R4** | 手机浏览器对本地文件支持很差（iOS Safari 几乎打不开本地文件） | PRD 里的"次要场景"可能不成立 | 维持 PRD 已定调：不专门投入，能用即可。需要手机看的话，等第 4 周上线之后用网址访问 |
| **R5** | 不用框架，所有界面逻辑手写，代码量会慢慢涨 | Day 8 之后可能变乱 | 已按 `config / store / rules / ui` 分层；如果某天觉得乱，先拆文件，不要急着引框架 |

### 8.2 还留着没定的（不影响 Day 7）

| 编号 | 待定项 | 什么时候定 |
| --- | --- | --- |
| U1 | 要不要加"导出时顺手存进一个固定文件夹"（File System Access API 增强） | 用满两周后，如果觉得每周导出太麻烦再说 |
| U2 | 数据要不要自动带上"记录时的天气/位置"之类 | **不做**，违反"字段越少越好"的 PRD 决定 |
| U3 | 手机上到底能不能用 | 第 4 周上线后自然有答案 |

### 8.3 明确不在本期

- 不引入任何框架、构建工具、包管理器；
- 不写单元测试（技术选型阶段判断：功能少、界面为主，手动按 TC 逐条自查更划算）；
- 不做多语言、不做主题切换、不做暗色模式；
- 不接数据库、不接云端——那是 Day 15 之后的事。

---

## 9. 与后面几天的衔接

| 哪天 | 要做什么 | 和本文档的关系 |
| --- | --- | --- |
| **Day 6** | 写 `AGENTS.md` | 把本文档**第 5 节那 9 条约定**抄进去，变成给 AI 的硬规则。`TECH_DESIGN.md` 是写给人看的说明书，`AGENTS.md` 是写给 AI 看的施工守则 |
| **Day 7** | 开发第一个能跑的 MVP | 按 3.5 的文件结构建；开发顺序：`config.js` → `store.js` → `rules.js` → `index.html` 静态结构 + `style.css` → `ui.js` 渲染 → 弹窗表单 → 导出/导入 → `source.html` |
| **Day 7 第一件事** | **亲手点一次「导出」**，把生成的文件双击打开确认可读 | 补上 6.5 表里那条"还没证明"的实测（PRD R1 的收尾） |

---

## 附录 A　术语表（大白话）

| 词 | 大白话解释 |
| --- | --- |
| `file://` | 双击打开本地网页时，浏览器地址栏里的那种地址。它和"https:// 网站"是两种不同的来源 |
| 来源（origin） | 浏览器的"地盘"概念：同一个地盘里的页面能共享数据，不同地盘的不能 |
| localStorage | 浏览器给每个"地盘"分的一小块储物柜，关掉浏览器再打开东西还在。**它不是文件**，所以单独靠它不满足 C2 |
| Blob | 一段"内存里的文件内容"。网页可以把数据变成 Blob，再让它下载到磁盘 |
| `type="module"` | 一种更现代的 JS 写法。**本地双击打开的页面用不了它**（实验 3 已证明） |
| CORS | 浏览器的安全规矩：不让一个页面随便去读另一个地盘的东西。`file://` 下模块脚本被拦就是撞在这条规矩上 |
| schemaVersion | 数据文件的"格式版本号"。以后格式变了，靠它判断旧文件还能不能读 |
| 纯函数 | 给同样的输入永远得到同样的输出、并且不去改外部东西的函数。`rules.js` 里全是这种，最好验证 |
| 无头模式（headless） | 让浏览器不显示窗口、在后台跑完一个网页。本文档第 6 节的实验就是这么跑的 |

---

## 附录 B　AI 自检记录（Day 5 板块③）

**自检方式**：先按 Day 5 的完成标准核对本文档，再问四个问题——
① T1–T4 四个待定项是否都给了明确结论和理由？② 有没有哪个决定违反了 C1/C2/C3？
③ 有没有偷偷引入"需要装东西 / 需要联网"的东西？④ 每条技术结论是不是**有实测支撑**，而不是我猜的？

除了人眼通读，还用脚本做了机器核验：章节结构、TC 编号是否连续（TC-01→TC-12 ✓）、
问题编号（Q1→Q9、V1→V3 ✓）、加粗标记是否配对、**全文的 `https://` 出现位置逐个过一遍**
（结果：11 处全部是"仓库地址 / 举例说明什么被禁止 / 我自己填进去的链接地址"，
**没有一处是真的资源引用**）。

### B.1 第一轮自检：发现的问题

| 编号 | 发现的问题 | 级别 | 处理 |
| --- | --- | --- | --- |
| Q1 | 初稿打算用 ES 模块（`type="module"`）拆文件，看起来更"专业"。但实验 3 实测：**`file://` 下模块脚本会被浏览器拦住**，页面会直接白屏 | **必须修改** | 改为传统 `<script src>` 按顺序加载，并把这条写成第 5 节第 1 条硬规矩 + TC-10 检查项 |
| Q2 | 初稿的时间线排序只按 `recordedAt`。但 PRD 4.4 明确「记录时间允许手改」——一改就可能出现两条时间完全相同，顺序会飘，违反 AC-07「同一天的多条按保存先后，不能乱」 | **必须修改** | 数据里加一个技术字段 `savedAt`，排序规则改为「先 `recordedAt` 降序，再 `savedAt` 降序」，见 5.3 |
| Q3 | 初稿把弹窗的 HTML 在两个页面里各写一遍 | **必须修改** | 改由 `ui.js` 统一注入，避免"改一处忘一处"，见 3.5 |
| Q4 | 「导入」是唯一会**覆盖并清空**现有数据的操作，初稿只写了"二次确认"。考虑到 C2 的前提是"删掉真的找不回来"，只弹一个确认框不够稳 | **必须修改** | 改为**导入前先自动导出一份当前数据当备份**，再执行覆盖（TC-07 同步更新） |
| Q5 | 初稿把 7 天、5 个上限、存储键名散落在各文件里 | 建议修改 | 全部收进 `config.js`，成 T4 的答案，并写进 TC-08 |
| Q6 | 初稿没写"换浏览器/清缓存会丢数据"这件事 | 建议修改 | 补进 3.3 的"已知代价"和风险 R1 |
| Q7 | 初稿回避了 C1 与 Day 15 "云端数据库"的方向冲突 | 建议修改 | 显式写成风险 R2，并给出 Day 15 要做的二选一决策和"改 `store.js` 一个文件"的准备 |
| Q8 | 初稿写了"以后可以考虑用 File System Access API 直接读写文件"但没说清为什么现在不用 | 建议修改 | 在 3.3 补上三条放弃理由（浏览器兼容、每次弹对话框破坏高频操作、句柄失效），并挪进 8.2 作为可选增强 |
| Q9 | 是否引入 TypeScript、是否写单元测试 | 可以忽略 | 功能少、界面为主，手写 JS + 按 TC 自查更划算，已写进 8.3 |
| Q10 | **交叉引用指错了章节**：正文两处写"见 5.1"，但 5.1 是「X 天前怎么算」那一小节，而想引用的是第 5 节的第 1 条规矩。按着错误引用跳过去会读到不相干的内容 | **必须修改** | 两处都改为「第 5 节第 1 条」（6.4 节正文 + TC-10）。这一条是**机器核验查出来的**，通读时没发现——说明"逐条数编号、逐条追引用"这一步不能省 |

### B.2 第二轮自检：结果

| 检查项 | 结论 |
| --- | --- |
| T1–T4 是否都有明确结论？ | **是。** T1→3.3（localStorage + 导出文件）；T2→3.1 + 3.5（双击本地文件；2 个 HTML + 共享 assets）；T3→3.4（JSON 格式与字段表）；T4→3.6（`config.js` 一处常量） |
| 有没有违反 C1？ | **没有，而且主动加固了。** 全项目零在线资源、零构建步骤、零服务；并把"页面里不许出现 `https://` 资源"变成可检查的 TC-03 |
| 有没有违反 C2？ | **没有。** 而且这一条**已经不靠推测**：实验 1b 真的生成了可读的 JSON 文件（6.2） |
| 有没有违反 C3？ | **没有。** 上限 5 由 `config.js` 一处控制，TC-08 检查 |
| 有没有引入"要装东西 / 要联网"的东西？ | **没有。** 不装 Node 包、不用 CDN、不用在线字体图标 |
| 每条技术结论有没有实测支撑？ | **有。** 第 6 节四个实验都跑了并留了证据；剩下没证明的（真实点按钮的完整流程、手机）也**明确列在 6.5 表里**，不装作已完成 |
| 和 PRD 有没有冲突？ | **没有冲突，但发现一处未来的方向冲突**：C1（不依赖服务器）与课程 Day 15 起的云端数据库。已作为风险 R2 明确记录，并给出处理路径，而不是假装不存在 |
| 关键数字 / 命名是否唯一？ | **是。** 阈值 7、上限 5、存储键名、schemaVersion 各只有一处定义（`config.js`） |

**第二轮自检结论：0 个「必须修改」级问题。**

### B.3 仍然存在的已知不确定性（诚实记录）

| 编号 | 事项 | 为什么不升级为「必须修改」 |
| --- | --- | --- |
| V1 | 真实点击导出的完整人工流程要 Day 7 才能补测 | 实验 1b 已经从等价路径验证过（文件确实生成了、内容确实可读），剩下的是"亲眼确认一次"，属于 Day 7 第一件事 |
| V2 | 手机浏览器的表现完全未知 | PRD 已把手机定为"能用即可"，不在本期验收范围内 |
| V3 | 不用框架的长期代价（代码量上涨） | 已用分层（`config/store/rules/ui`）缓解，并把"先拆文件不要急着引框架"写成 R5 的应对 |

---

## 附录 C　与 `PRD.md` 待定项的对应关系

| PRD 待定项 | 结论落在本文档哪里 |
| --- | --- |
| T1 数据具体怎么存 | 3.3 —— localStorage 日常存 + 导出 JSON 成文件 |
| T2 3 个页面做成 1 个还是 3 个 | 3.1 + 3.5 —— 2 个 HTML 文件（弹窗不算页面）+ 共享 `assets/` |
| T3 数据文件的格式和字段名 | 3.4 —— JSON，字段表 + 完整样例 |
| T4 7 天阈值写在哪 | 3.6 —— `assets/config.js` 一处常量 |
| R1 数据能直接拷走这条要验证 | 6.2 —— 实验 1b 实测通过，R1 风险解除 |

---

**文档状态**：Day 5 产出（2026-09-20）｜v1.1，已通过 AI 自检（0 个必须修改级问题）
**下一步**：Day 6 写 `AGENTS.md`（把本文档第 5 节的 9 条约定变成给 AI 的施工守则），随后 Day 7 按 3.5 的结构开工。
